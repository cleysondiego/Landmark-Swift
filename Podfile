# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
source 'https://git-codecommit.us-east-1.amazonaws.com/v1/repos/release-mobile-ios-specs' # Heartbeat
source 'https://cdn.cocoapods.org' # default

target 'Landmarks' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Landmarks
  pod 'Heartbeat'

end

target 'MacLandmarks' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MacLandmarks

end

target 'WatchLandmarks' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WatchLandmarks

end

target 'WatchLandmarks WatchKit Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WatchLandmarks WatchKit Extension

end
